一、Python 是什么样的语言（一句话认知）

解释型（不用编译）

动态类型（变量不用声明类型）

靠缩进表示代码块（非常重要）

二、最基础的语法结构
1️⃣ 输出 / 打印
print("hello world")
print(123)
print("a", 1, True)


👉 相当于 Java 的 System.out.println

2️⃣ 变量（没有类型声明）
a = 10
b = "hello"
c = True


❗ 注意：

变量类型可以随时变

Python 不关心你“之前是什么类型”

a = 10
a = "abc"   # 合法

3️⃣ 基本数据类型
int      # 整数
float    # 浮点数
str      # 字符串
bool     # 布尔
None     # 空（类似 null）

x = None

三、字符串（比 Java 灵活）
s = "hello"
s2 = 'world'

字符串拼接
name = "Tom"
age = 18
print(name + str(age))

⭐ 推荐：f-string（非常好用）
print(f"name={name}, age={age}")

四、条件判断（没有括号，没有大括号）
if a > 10:
    print("大于10")
elif a == 10:
    print("等于10")
else:
    print("小于10")


⚠️ 冒号 + 缩进是语法的一部分

五、循环（重点）
1️⃣ for 循环（最常用）
for i in range(5):
    print(i)


👉 输出：0 1 2 3 4

for i in range(1, 10, 2):
    print(i)

2️⃣ 遍历集合
lst = [1, 2, 3]

for x in lst:
    print(x)

3️⃣ while 循环
i = 0
while i < 5:
    print(i)
    i += 1

六、集合类型（非常重要）
1️⃣ list（类似 ArrayList）
lst = [1, 2, 3]
lst.append(4)
lst[0] = 10

2️⃣ dict（类似 Map）
mp = {"id": 1, "name": "Tom"}
print(mp["id"])


安全取值：

mp.get("age")        # None
mp.get("age", 18)   # 默认值


遍历：

for k, v in mp.items():
    print(k, v)

3️⃣ set（去重集合）
s = {1, 2, 3}
s.add(4)

七、函数（比 Java 简单）
def add(a, b):
    return a + b


调用：

res = add(1, 2)

可选参数 / 默认值
def hello(name="world"):
    print(name)

hello()
hello("Tom")

八、类和对象（简化版 Java）
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def say(self):
        print(self.name, self.age)


使用：

p = Person("Tom", 18)
p.say()


⚠️ self ≈ Java 的 this

九、异常处理
try:
    x = 1 / 0
except Exception as e:
    print(e)
finally:
    print("结束")

十、模块和导包
import math
print(math.sqrt(4))

from math import sqrt
print(sqrt(4))

十一、Python 和 Java 的关键差异（你一定要记）
点	Python	Java
类型	动态	静态
代码块	缩进	{}
集合	内置强	需要库
语法	简	严
编译	无	有
十二、学习顺序建议（很重要）

你如果不是纯 Python 工程师，按这个顺序学就够了：

1️⃣ 基础语法（你现在这个）
2️⃣ list / dict / set
3️⃣ 函数
4️⃣ 文件操作
5️⃣ 第三方库（requests / numpy / torch 之类）

不用死磕“语法大全”。

一句话总结

Python 语法少、规则简单，真正的难点不在语法，而在库和应用场景